name = "rise-shine-evolve-worker"
main = "cloudflare-worker.js"
compatibility_date = "2024-12-01"

[vars]
# These are placeholders; set real secrets in the Workers dashboard instead of committing them.
# Set BREVO_API_KEY as an encrypted secret (wrangler secret put BREVO_API_KEY) before deploying.
BREVO_LIST_ID = "3"
